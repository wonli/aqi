[Unit]
Description={{.Description}}
After={{.After}}

[Service]
User={{.User}}
WorkingDirectory={{.WorkingDirectory}}
RestartSec={{.RestartSec}}
Type=simple

Environment=AQI_ENV=prod
ExecStart={{.ExecStart}}
Restart=always

[Install]
WantedBy=multi-user.target
